export const Form = () => {
  return (
    <form>
      <input type="text" placeholder="Full Name.." />
      <input type="text" placeholder="Email.." />
      <input type="numb" placeholder="Age.." />
      <input type="password" placeholder="Password..." />
      <input type="password" placeholder="Confirm Password.." />
      <input type="submit" />
    </form>
  );
};

export default Form;
